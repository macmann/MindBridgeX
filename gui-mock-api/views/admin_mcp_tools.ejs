<%
  const pageTitle = title || `MCP Tools · ${mcpServer?.name || 'MCP Server'}`;
  const body = include('partials/admin_mcp_tools_body', {
    mcpServer,
    tools,
    existingApis,
    key,
    mcpPath,
    mcpUrl,
    openapiPreview,
    rawOpenapiSpec,
    error
  });
%>
<%- include('layout', { title: pageTitle, body, adminKey: key }) %>
